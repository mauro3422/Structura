# v1.2.22 - Limpieza final de navegador y favicon

## Cambios

- Agregué un `favicon.ico` para eliminar el 404 que aparecía al abrir el laboratorio en navegador real.
- Corregí el `title` y la `meta description` del documento para que la pestaña del navegador muestre texto limpio.
- Validé en Playwright un flujo real del laboratorio con varias tablas, una relación 1:N y una tabla puente para N:N.

## Validación

- `npm run build`
- Playwright CLI con navegador real
- `console error` sin errores
